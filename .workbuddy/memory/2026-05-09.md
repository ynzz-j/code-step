# 2026-05-09

## CodeStep 架构稳定化执行

### 完成的变更
1. **typecheck 修复**（5 个错误→0）：
   - `CoursesPage.tsx`：移除未使用的 `ALL_DIFFICULTIES`、`Difficulty` 类型导入，移除 `useState`/`invoke` 导入，删除 `debugInfo` state 及 `debug_courses` useEffect
   - `CategoryFilter.tsx`：移除未使用的 `Difficulty` 类型导入
   - `TypingEditor.tsx`：移除未使用的 `handleContainerClick` 回调
2. **调试调用清理**：
   - `courseService.ts`：`getCourses()` 中移除 `debug_courses` invoke 和 console.log
   - `CoursesPage.tsx`：移除自动 `debug_courses` useEffect（Rust 侧 `debug_courses` 命令保留供开发排障）
3. **Tauri 命令契约收敛**：
   - `courseService.ts`：移除无调用点的 `checkCourseUpdates()` 方法和 `CourseUpdate` 接口
   - 确认 Rust 侧 `check_course_updates` 存在但未注册到 invoke_handler（死代码，后续清理）
4. **store 拆分边界标注**：
   - `courseStore.ts` 顶部增加 TODO 注释，标注 4 个拆分方向：courseCatalogStore、courseProgressStore、typingStatsStore、comboStore
5. **架构文档漂移标记**：
   - `docs/architecture-design.md` 顶部增加漂移提示，标注未落地模块（envStore/envService/CodeEditor/userStore 等）

### 验证结果
- `npm run typecheck`：✅ 0 错误
- `npm run build`（vite build）：✅ 71 modules, 2.04s
- `cargo check`：✅ 通过（14 个已有 snake_case warning，无新增）
