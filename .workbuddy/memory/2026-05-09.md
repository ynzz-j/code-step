# 2026-05-09 工作日志

## CodeStep Java 课程重构

按 `codestep-course-dev` skill 规范重构了 Java 课程目录：

### coding/java (Coding 模式 - 编程练习)
- **8 门课程，共 59 步骤**
- 修复：目录结构 `steps/{course-id}/steps/step-XX.json`
- 修复：course.json 移除顶级字段，只保留 courses 数组
- 课程：java-hello(5), java-vars(10), java-if-for(7), java-method(6), java-array(6), java-oop(12), java-collection(9), java-exception(4)

### typing/java (Typing 模式 - 打字练习)
- **17 门课程，共 69 步骤**
- 修复：从 coding 移入的 24 个 typing 步骤已重新编号
- 修复：course.json 添加 totalSteps 字段，添加缺失课程
- 新增课程：java-generics(4), java-lambda(3), java-stream(4)

### 规范要点
- course.json 顶级只能是 `courses` 数组，不能有其他字段
- 步骤路径格式：`steps/{course-id}/steps/step-01.json`
- 每个课程目录内步骤从 step-01 连续编号
