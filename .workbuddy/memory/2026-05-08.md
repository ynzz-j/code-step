# 2026-05-08 工作记录

## 修复 CodeStep 三个问题

### 1. 学习中心不显示课程（user_center.rs）
- 问题：`get_courses_dir()` 查找 `java-typing` 目录（旧结构），实际目录是 `courses/typing/`
- 修复：添加 `get_mode_dir()` 与 `course.rs` 保持一致；重写 `scan_course_metadata()` 正确解析语言包格式

### 2. 学习中心添加筛选功能（UserCenterPage.tsx）
- 添加语言筛选（Java/Python/JavaScript/C++）
- 添加状态筛选（全部/进行中/已完成）
- 实时过滤课程列表

### 3. 打字提前结束课程（TypingEditor.tsx）
- 修复事件处理函数里错误返回 JSX 的 bug
- 简化完成检测：`typed === step.targetCode`（移除冗余的 cursorPosition 检查）
- 添加 `completedRef` 防止 `onComplete()` 重复触发

## 下一步
- 需要编译 Rust 后端（`cargo build`）和前端测试
- `cargo` 在 bash 中不可用，需用 PowerShell/CMD 执行
