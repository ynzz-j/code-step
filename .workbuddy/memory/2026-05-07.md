# 2026-05-07 工作记录

## CodeStep Typing 模式修复

### 修复内容
1. **TypingEditor.tsx** - 复用 `useTypingStats` hook 和 `StatsPanel` 组件显示统计

2. **useTypingStats.ts** - 更新匹配 `TypingStats` 类型：
   - 添加 `totalKeystrokes` 和 `correctKeystrokes` 字段

3. **course.rs** - Rust 后端课程路径修复：
   - 修复 `get_courses_dir()` 从 `target/debug` 向上查找项目根目录
   - 支持多种路径组合：`../..`、`../../..`、从 EXE 所在目录向上查找
   - 课程目录正确指向 `courses/java-typing/`

4. **progress.rs** - 新增 `CourseProgressResponse` 结构体

### 重要：Tauri 开发模式路径问题
- 问题：`std::env::current_dir()` 返回 `src-tauri/target/debug/` 而非项目根目录
- 解决：从 EXE 路径向上查找 `courses/` 目录

### 二期切换方式
- 前端：`CURRENT_COURSE_MODE = 'typing'` → `'coding'`
- 后端：`CURRENT_MODE` 配置常量
- 课程目录：`courses/java-typing/` → `courses/java/`

---

## 难度筛选 & 学习进度修复

### 难度筛选功能
- **types/course.ts** - 添加 `DIFFICULTY_LABELS` 和 `ALL_DIFFICULTIES` 常量
- **stores/courseStore.ts** - 添加 `selectedDifficulty` 状态和 `setDifficulty` 方法
- **components/courses/CategoryFilter.tsx** - 添加难度筛选按钮（入门/基础/困难）
- 难度颜色：入门(绿)、基础(蓝)、困难(橙)

### 用户中心学习进度修复
- **src-tauri/src/commands/user_center.rs** - 修复 `get_courses_dir()` 函数
- 统一使用与 `course.rs` 相同的路径计算逻辑：从 `current_exe()` 向上 4 层找到项目根
- 修复前：`current_dir()` 返回 `src-tauri/target/debug/`（找不到课程）
- 修复后：从 EXE 路径正确解析到项目根目录
